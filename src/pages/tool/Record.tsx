import { AppInfo } from "../../types/interfaces";

const Record: React.FC = () => {
	return (
		<>
			<div style={{ height: "100%", width: "100%" }}>녹음기</div>
		</>
	);
};

export const config: AppInfo = {
	name: "Record",
	// icon: "images/3d/3d-coffee-cup.png",
};

export default Record;
